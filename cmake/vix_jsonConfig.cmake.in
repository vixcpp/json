@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

if (NOT @VIX_JSON_BUNDLED@)
  find_dependency(nlohmann_json CONFIG QUIET)
endif()

include("${CMAKE_CURRENT_LIST_DIR}/vix_jsonTargets.cmake")

check_required_components(vix_json)
